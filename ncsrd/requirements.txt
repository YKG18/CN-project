# Core — required by prep.py and adapter.py
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# Optional — imported lazily, only when you use the matching feature
imbalanced-learn>=0.11    # balance="smote"
xgboost>=2.0              # for_xgboost()
tensorflow>=2.13          # for_mlp() / for_cnn() / for_lstm()  (or: keras>=3.0)
# torch>=2.0              # PyTorch instead of TensorFlow
# shap>=0.44              # XGBoost feature attribution
# matplotlib>=3.7         # plots
# seaborn>=0.13           # confusion-matrix heatmaps
