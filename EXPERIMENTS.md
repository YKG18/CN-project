# Experiment log

One row per run. Owner: M5, but every member appends their own runs.
Every run records its configuration and seed (Implementation.md §3.2, rule 4).

`split_policy` is `reference` or `project_standard`;
`config` names the method variant, e.g. `base_paper`, `base_saurabh_static`.

| Date | Member | Dataset | Method | Config | Feature set | Split policy | Seed | Result file | Notes |
|---|---|---|---|---|---|---|---|---|---|
| | | | | | | | | | |
