# Frozen shared environment for the whole group (Phase 0, Implementation.md 3.3).
# Python 3.10-3.12. Install with:  pip install -r requirements.txt
#
# Do not add per-member pins here without telling the group -- everyone must
# train and evaluate on the same library versions.

# --- core: needed by every module -------------------------------------
numpy>=1.24,<3
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
joblib>=1.3

# --- models -----------------------------------------------------------
xgboost>=2.0
imbalanced-learn>=0.11        # SMOTE (Saurabh baseline reproduction)

# --- explainability (M2 Module C, M3 lightweight SHAP) ----------------
shap>=0.44

# --- plots / reporting ------------------------------------------------
matplotlib>=3.7
seaborn>=0.13

# --- notebooks --------------------------------------------------------
jupyter>=1.0

# --- optional: deep learning (M1 optional CNN/LSTM/MLP, M3 distillation)
# Uncomment only if you actually run them; they are large installs.
# tensorflow>=2.13
# torch>=2.0
